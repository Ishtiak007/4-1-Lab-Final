pip install numpy matplotlib scikit-learn tensorflow

python filename.py



==========================Question==================

1. Prepare a PDF report using any LaTeX software. In the report, design and implement a 
Fully Connected Feed-forward Neural Network (FCFNN) to learn and solve the linear 
equation: 
y = 7x + 12 
Create suitable training, validation, and testing datasets for this equation. Train the model 
using your prepared data, test its performance, and compare the actual output values with 
the predicted output values using a proper plot. 


2.Write a PDF report using any LaTeX software where you develop an FCFNN model for 
the following quadratic function: 
y = 2x² + 6x + 8 
For this task, prepare separate datasets for training, validation, and testing. After training 
the neural network, evaluate the model with the test data and show a comparison between 
the original y values and the predicted y values through graphs. 


3. Using any LaTeX software, create a PDF report on building a Fully Connected 
Feed-forward Neural Network (FCFNN) for the cubic equation: 
y = 5x³ + 2x² + 4x + 9 
Prepare the necessary training, validation, and test data for the equation. Train the 
network, test its accuracy, and present the difference between the real y values and the 
model’s predicted y values using visual plots. Also include a short discussion on how the 
cubic power of x may influence the model structure and the amount of data required. 


4. Prepare a PDF report using any LaTeX software. In the report, design a Convolutional 
Neural Network (CNN) model for a 10-class classification problem. Train and test the 
model using the following datasets: 
i. Fashion MNIST 
ii. MNIST English 
iii. CIFAR-10 


5. Write a report in pdf format using any Latex system after:  
→ training a binary classifier, based on the pre-trained VGG16, by transfer learning 
and fine tuning.  
→ showing the effect of fine-tuning:  
i. whole pre-trained VGG16  
ii. partial pre-trained VGG16 
